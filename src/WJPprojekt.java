/**
 * Klasa spajająca wszystko w całość - wywołanie inicjalizacji
 */
public class WJPprojekt {

    /**
     * Funkcja main programu - inicjalizacja
     * @param args
     */
    public static void main(String[] args)  {
        initWindow init = new initWindow();
        init.initialize();
    }
}
